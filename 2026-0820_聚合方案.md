# 学习中心聚合方案（2026-08-20）

> 现状诊断 → 架构梳理 → 分批聚合计划 → 质量修复策略
> 作者：同志 ｜ 审阅：雪薇

---

## 一、生成程序定位

### 1.1 核心生产系统：spdt-content-cards

**位置**：`D:\B_历史\spdt-content-cards\`（Git 仓库）

这是整个知识卡片体系的"本征层"——由另一台电脑上的生产 AI（代号 autoclaw）按规范 v1.0 产出 JSON 卡片，本机管家（代号 mavis）负责拉取、校验、同步。

```
D:\B_历史\spdt-content-cards\
├── 00-项目文档\
│   ├── MANIFEST.yaml                     ← 项目状态契约（总账）
│   ├── _62_concepts.json                 ← 历史 62 概念清单
│   ├── 知识卡片产出规范_v1.0_给AI的指令.md   ← 生产规范
│   └── 内容项目架构_v2_规划_2026-08-16.md
├── _tools\
│   ├── ru_cardpkg_convert.py             ← → rujing APP 转换器
│   ├── _validate_cards.py               ← 卡片校验器（20 项硬指标）
│   └── push_to_rujing.ps1               ← 推送脚本
├── 历史\
│   ├── _62_concepts.json
│   └── cards\  (62 个概念目录，每个含 chain.json + main.json + K01~K07.json)
├── 地理\
│   ├── _50_concepts.json
│   └── cards\  (49 个概念目录)
└── 政治\
    ├── _61_concepts.json
    └── cards\  (61 个概念目录)
```

### 1.2 卡片目录结构（每个概念 = 1 套）

```
cards\2026-08-16_洋务运动\
├── chain.json    ← 链元数据（学科/模块/专题/主线标记/状态）
├── main.json     ← 主卡（front=问题, back_core=核心要点, back_detail=叙事正文, exam_questions[5~6], open_questions[5~10]）
├── K01.json      ← 子卡 1（front=考点问题, back_core=要点, back_detail=详解+易错点）
├── K02.json
└── ...K07.json
```

### 1.3 相关工具

| 工具 | 路径 | 用途 |
|------|------|------|
| 卡片校验器 | `_tools\_validate_cards.py` | 20 项硬指标 + 概念清单交叉核对 |
| rujing 转换器 | `_tools\ru_cardpkg_convert.py` | JSON → rujing APP CardPackage 格式 |
| **HTML 转换器（新建）** | `D:\Z_学习平台\json2html_converter.py` | **JSON → 学习中心 HTML DATA 格式（本次新增）** |

### 1.4 其他相关项目

| 项目 | 路径 | 关系 |
|------|------|------|
| GaokaoPrep | `D:\Dev\GaokaoPrep\` | 独立的数学题库项目，与当前体系无关 |
| 三件套 Markdown | `D:\B_历史\三件套\` 等 | 人工精写的高质量版本，仅覆盖少量概念 |

---

## 二、数据盘点

### 2.1 JSON 卡片库存量（本征层）

| 学科 | 概念数 | 子卡数 | 校验状态 | 最后产出日期 |
|------|--------|--------|----------|-------------|
| 历史 | 62 | 379 | ✅ 全 PASS | 2026-08-17 |
| 地理 | 49 | 294 | ✅ 全 PASS | 2026-08-19 |
| 政治 | 61 | 366 | ✅ 全 PASS | 2026-08-19 |
| **合计** | **172** | **1039** | | |

### 2.2 三件套 Markdown（人工精写版）

| 学科 | 文件数 | 位置 |
|------|--------|------|
| 历史 | 7 | `D:\B_历史\三件套\` |
| 地理 | 19 | `D:\C_地理\三件套\` |
| 政治 | 23 | `D:\D_政治\三件套\` |
| 古诗文 | 3 | `D:\E_古诗文\三件套\` |
| 英语 | 3 | `D:\F_英语\三件套\` |
| **合计** | **55** | |

### 2.3 学习中心 HTML 当前状态

**文件**：`D:\Z_学习平台\学习中心.html`（4148 行）

**TREES（侧边栏结构）概念统计**：

| 学科 | TREES 概念数 | 有内容(绿点) | 待产(灰点) | 完成率 |
|------|-------------|-------------|-----------|--------|
| 历史 | 62 | 62 | 0 | 100% |
| 地理 | 49 | 49 | 0 | 100% |
| 政治 | 61 | 61 | 0 | 100% |
| 古诗文 | 16 | 3 | 13 | 19% |
| 英语 | 18 | 3 | 15 | 17% |
| **合计** | **206** | **178** | **28** | **86%** |

**DATA 条目统计（实际内容）**：

| 学科 | DATA 条目数 | 数据来源 | 质量 |
|------|-----------|---------|------|
| 历史 | 64 | 7 件人工精写 + 57 件卡包初录 | 精写部分优 / 卡包部分有缺陷 |
| 地理 | 49 | 全部卡包初录 | 有缺陷 |
| 政治 | 62 | 部分精写 + 多数卡包初录 | 精写部分优 / 卡包部分有缺陷 |
| 古诗文 | 3 | 全部人工精写 | 优 |
| 英语 | 3 | 全部人工精写 | 优 |
| **合计** | **181** | | |

---

## 三、质量问题诊断

### 3.1 "卡包初录"导入缺陷（已定位）

2026-08-19 执行的"卡包初录"批量导入存在三类质量问题，**根因在 JSON 源数据**（生产 AI 的重复文本 bug）：

| 缺陷 | 表现 | 影响范围 | 原因 |
|------|------|---------|------|
| **补充说明重复** | 同一段"补充说明"文本重复 8~11 次 | 地理49 + 政治多数 + 历史部分 | 生产 AI 陷入循环重复 |
| **这一问考的是什么重复** | K 卡 front 中"这一问，考的是什么？"重复 1~3 次 | 地理 + 政治 全部 K 卡 | 生产 AI 模板填充 bug |
| **填充句重复** | "理解这一点，是掌握本考点的关键所在。"重复 2~3 次 | 政治（34 处）+ 历史（少量） | 生产 AI 循环 |
| **内容截断** | 卡片正文在句中截断 | 地理 + 政治 部分卡片 | 初录导入未做全文映射 |

### 3.2 缺失学科

| 学科 | 状态 | 缺口 |
|------|------|------|
| 古诗文 | 仅 3/16 概念入库 | 13 个概念待产，无 JSON 卡片生产线 |
| 英语 | 仅 3/18 概念入库 | 15 个概念待产，无 JSON 卡片生产线 |
| 数学 | 0 概念 | 仅有框架草案（挂账） |

---

## 四、聚合方案

### 4.1 总体策略

```
第一优先：用转换器重建三科 DATA（历史/地理/政治）——修质量
第二优先：扩展 TREES + 产古诗文/英语 JSON 卡片——补缺口
第三优先：数学启动框架——挂账转实产
```

### 4.2 第一步：重建三科 DATA（立即执行）

**目标**：用 `json2html_converter.py` 重新生成历史/地理/政治三科 DATA，替换有缺陷的"卡包初录"版本。

**已产出文件**：

| 生成文件 | 大小 | 概念数 | 清洗后质量 |
|---------|------|--------|-----------|
| `_generated_history.js` | 210 KB | 62 | ✅ 干净 |
| `_generated_geography.js` | 159 KB | 49 | ✅ 干净 |
| `_generated_politics.js` | 224 KB | 61 | ✅ 干净 |

**清洗效果对比**：

| 指标 | 旧版（卡包初录） | 新版（转换器） |
|------|----------------|--------------|
| "补充说明"重复 | 8~11 次/概念 | ≤1 次/概念 |
| "这一问，考的是什么？" | 1~3 次/卡 | 0 次 |
| "理解这一点…关键所在" | 2~3 次/卡 | 0 次（保留自然用法） |
| 卡片正文截断 | 有 | 无 |

**执行方法**：

1. 备份当前 HTML（`学习中心.html.bak_20260820`）
2. 用 `_generated_*.js` 中的干净 DATA 条目替换 HTML 中对应的旧"卡包初录"条目
3. 保留 HTML 中"已过审"的人工精写条目不动（质量已达标）
4. 测试打开 HTML 验证渲染正常

**保留策略**：对每个概念，优先用人工精写版（"已过审"），精写版不存在时用转换器版。

### 4.3 第二步：扩展古诗文 + 英语（中期）

**古诗文**（缺口 13/16）：

| 阶段 | 内容 | 数量 |
|------|------|------|
| 阶段 A | 实词簇（20 簇批量） | 覆盖实词·过、一词多义等 |
| 阶段 B | 虚词句式 | 重点虚词(18)、文言句式、翻译六字诀 |
| 阶段 C | 诗词母题 | 常见意象、题材母题、表达技巧、语言风格 |

**英语**（缺口 15/18）：

| 阶段 | 内容 | 数量 |
|------|------|------|
| 阶段 A | 非谓语 + 三大从句 | 不定式、动名词、分词、名词性从句、状语从句 |
| 阶段 B | 特殊结构 + 词汇 | 虚拟语气、倒装、情态动词、高频词簇等 |
| 阶段 C | 题型策略 | 完形填空、选词填空策略 |

**生产路径**：需在 spdt-content-cards 中新建 `古诗文/cards/` 和 `英语/cards/` 目录，按 v1.0 规范产 JSON 卡片，然后用转换器导入。

### 4.4 第三步：数学启动（长期）

- 当前仅有 `D:\G_数学\框架\2026-0816_真题解剖报告（挂账版）.md`
- 需先完成概念清单（`_55_concepts.json`），再启动卡片生产

---

## 五、转换器使用说明

### 5.1 json2html_converter.py

**位置**：`D:\Z_学习平台\json2html_converter.py`

**功能**：读取 spdt-content-cards 中的 JSON 卡片集，自动转换为学习中心 HTML 的 DATA 格式，并清洗生产 AI 的重复文本 bug。

**用法**：

```bash
# 转换单个学科
python json2html_converter.py --subject 地理

# 转换全部已产出学科（历史+地理+政治）
python json2html_converter.py --all

# 指定输出路径
python json2html_converter.py --subject 历史 --output D:/path/to/output.js
```

**输出**：`D:\Z_学习平台\_generated_{subject}.js`

**清洗规则**：

| 规则 | 处理 |
|------|------|
| `补充说明：…` 重复 | 只保留首次出现 |
| `这一问，考的是什么？` | 全部删除 |
| `理解这一点，是[掌把]握本考点的关键所在。` | 全部删除 |
| `这里涉及的核心概念是XXX。` | 全部删除 |
| 完全相同的连续行 | 去重 |

**字段映射**：

| JSON 字段 | → | HTML DATA 字段 |
|-----------|---|---------------|
| `main.json.back_detail` | → | `body[]`（按空行分段，首段 lead:true） |
| `K*.json.front`（清洗后） | → | `cards[][1]`（考点） |
| `K*.json.back_core`（清洗后） | → | `cards[][2]`（核心要点） |
| `K*.json.back_detail` 中提取的易错点 | → | `cards[][3]`（易混辨析） |
| `K*.json.tags` 中 `#content/` | → | `cards[][4]`（角度/范式） |
| `main.json.exam_questions` | → | `quiz[]`（type + body + ans） |

### 5.2 导入 HTML 的操作步骤

1. 运行转换器生成 `_generated_*.js`
2. 打开 `学习中心.html`，定位到 `const DATA = {...}` 行（约第 1690 行）
3. 删除旧的"卡包初录"DATA 条目（保留"已过审"的精写条目）
4. 将 `_generated_*.js` 的内容粘贴到 DATA 定义之后
5. 保存，浏览器打开验证

---

## 六、里程碑

| 里程碑 | 内容 | 状态 |
|--------|------|------|
| **M0** | 诊断 + 转换器开发 | ✅ 完成（2026-08-20） |
| **M1** | 三科 DATA 重建（历史/地理/政治） | 待执行（替换 172 条） |
| **M2** | 古诗文 JSON 卡片生产 | 待启动（16 概念） |
| **M3** | 英语 JSON 卡片生产 | 待启动（18 概念） |
| **M4** | 数学概念清单 + 卡片生产 | 长期 |

---

*存档：D:\Z_学习平台\ ｜ 方案修订走审核循环*
